# 协作规范

## 人力预算：50 人时/周

2 人 × 5 小时/天 × 5 天 = **50 人时/周**。这是全部资源。 Allocation 建议：

| 用途 | 人时/周 | 说明 |
|---|---|---|
| 执行（上架、沟通供应商、处理订单） | 25 | 纯人力，Agent 替不了 |
| 与 Agent 协作（下任务、审方案、改产出） | 12 | Agent 是分析师，你是指挥官 |
| 学习（平台规则、类目知识） | 8 | 0→1 阶段必须投入 |
| 复盘与流程优化 | 5 | 每周五，别省 |

> 如果某周"执行"超过 30 人时，说明有流程该交给 Agent 了。

## 角色分工（建议）

按**供给侧 / 需求侧**切，而不是按平台切 —— 避免两人做同一件事的不同版本。

| 角色 | 负责 | 主要对接 Agent |
|---|---|---|
| **供给侧负责人** | 市场研究、选品、供应商、成本与利润 | Market, Competitor, Product, Finance |
| **需求侧负责人** | Listing、内容素材、广告投放、评价与客服 | Store, Content, Ads, Customer |

CEO / Compliance 由两人共管，每周经营会上一起决策。

> 每 2 周可以轮换一次，避免信息孤岛。

## 每日节奏（每人 5 小时）

| 时段 | 时长 | 内容 |
|---|---|---|
| 开工 | 15 min | 两人同步：昨天进度、今天 3 件最重要的事、卡点 |
| 上午块 | 2 h | 深度执行（不下任务给 Agent，自己干活） |
| 午间 | 30 min | 给 Agent 下任务 / 审 Agent 昨天的产出 |
| 下午块 | 2 h | 执行 + 处理 Agent 产出 |
| 收工 | 15 min | 更新黑板状态、记录待办、写 3 行日志 |

详见 [`workflows/daily.md`](workflows/daily.md)。

## 每周节奏

- **周五 1 小时经营会**：看指标 → 审 Agent 产出 → 决策 → 更新路线图。见 [`workflows/weekly.md`](workflows/weekly.md)
- **周五 30 分钟流程优化**：把本周重复做的事写成 SOP 或交给 Agent

## Git 流程（极简）

```
main          ← 永远可用，只接受来自 dev 的合并
└── dev       ← 日常开发
    └── feat/xxx   ← 单个任务，做完合并回 dev 后删除
```

规则：
- 分支命名：`feat/agent-market-v2`、`fix/profit-model`、`docs/weekly-2026-09-12`
- 提交信息：`<type>: <简述>`，type ∈ `feat | fix | docs | data | agent | chore`
  - 例：`agent: 更新 Market Agent 评分卡权重`
  - 例：`data: 导入 0912 市场扫描结果`
- 每天开工前 `pull`，收工前 `commit + push`
- **不要**在 main 上直接改

## 命名规范

| 对象 | 规范 | 示例 |
|---|---|---|
| Agent 文件 | 小写英文，无空格 | `market.md`、`competitor.md` |
| 输出文档 | `YYYYMMDD-类型-主题.md` | `20260912-report-market-selection.md` |
| 数据表 | `类型-范围.csv` | `market-scan.csv`、`sku-candidates.csv` |
| 决策记录 | 写进黑板 `decisions` 数组 + 周报 | — |
| 分支 | `类型/简述` | `feat/ads-agent` |

## 与 Agent 协作的三条纪律

1. **下任务要给足上下文**：背景 + 目标 + 约束（C1–C5）+ 期望产出格式。一句话任务只会得到一句话答案。
2. **Agent 的产出必须抽检**：尤其是数字。Agent 会算错、会编数据。凡是要花钱的决策，人工复核一遍。
3. **好的产出要沉淀**：如果某个 prompt 产出了好结果，把它存成 `workflows/` 里的一个流程，下次直接复用。

## 权限与安全

- 平台的账号密码、API Key **不进仓库**，放在各自的密码管理器
- 仓库里不放：供应商报价原件、真实成本表、店铺后台截图
  - 成本表用相对值或代号，原件放腾讯文档（权限可控）
- 仓库设为 **private**
